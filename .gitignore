# --- 민감 정보 (절대 커밋 금지) ---
.credentials.json
mcp-needs-auth-cache.json

# --- 기기별 로컬 상태 (세션, 캐시, 임시 파일) ---
backups/
cache/
debug/
downloads/
history.jsonl
ide/
plugins/
projects/
scheduled-tasks/
session-env/
sessions/
shell-snapshots/
tasks/
.last-cleanup
.last-update-result.json

# --- 기기별 설정 (윈도우 전용 경로/훅 포함 — 맥과 공유 불가) ---
settings.json
settings.local.json

# --- 중복 백업 ---
skills.backup.*

# --- OS / 에디터 ---
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*~

# --- 임시 파일 ---
*.tmp
*.log
*.bak
